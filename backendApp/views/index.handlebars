<div class="bg-light p-5">
  <h1 class="display-5">Bem-vindo ao Blog do Node!</h1>
  <p class="lead">Este é um BLOOG simples, desenvolvido em Nodejs + express + mongoDB!</p>
  <hr class="my-4">
  
  <p>Crie sua conta para realizar suas postagens em nosso BLOG.</p>
  <a class="btn btn-primary btn-lg" href="/usuarios/registro" role="button">Crie uma conta</a>
</div>

<hr class="mt-5">
<h3 class="mt-3">Postagens recentes:</h3>

{{#each postagens }}
<div class="card mt-4">
    <div class="card-body">
        <h3>{{titulo}}</h3>
        <p>{{descricao}}</p>
        <a href="/postagem/{{slug}}"><button class="btn btn-success">Leia mais</button></a>
        <hr>
        <small>Categoria: {{categoria.nome}}</small> <br>
        <small>Data de publicação: {{data}}</small>
    </div>
</div>

{{else}}
    <h5>Nenhuma postagem!</h5>
{{/each}}